DROP TABLE goals;

CREATE TABLE goals (
    goalsid SERIAL PRIMARY KEY NOT NULL,
    goalname varchar(20),
    description varchar(80),
    startdate VARCHAR(11),
    enddate VARCHAR(11)
);


SELECT * FROM goals;